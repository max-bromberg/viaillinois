# Changelog

All notable changes to VIA are recorded here. Versions follow semantic versioning.

## Unreleased

## 0.8.0 (2026-09-21)

- 

- The front page, the organizations and the event pages stopped moving while they load. The page body had no height of its own, so it was nothing tall at first paint and the footer sat under the band and then dropped as the content arrived, taking everything below the fold with it. On the organizations page that measured 0.478 of layout shift, which Core Web Vitals calls poor, and it is 0.015 now.
- Asking VIA who is looking is no longer an error when nobody is. Every visitor's first request is that one, because the page cannot read the cookie holding the session, and the answer was 401 to anybody not signed in. A browser writes a failed request into the console whatever the page does about it, so every anonymous reader and every crawler that renders a page met a red error on the way in.
- A page VIA could not describe now answers 503 rather than 200. The page builders treated a failed query and an empty answer as the same thing, so a minute of the database being away served noindex for every event on the site, and noindex is the one instruction Google acts on immediately and takes weeks to undo.
- Three accessibility faults across the site. The day headings on the agenda and the panels on an event skipped heading levels, so a screen reader moving by heading read missing sections where there were none. The Back and Next buttons on a listing, and the Auto stop on the theme control, were named something other than the word printed on them, so a voice control user saying what they could see reached nothing.
- The board drawn behind the page waits for a painted frame before it lays itself out. It was building a circuit and filling a canvas at the moment the browser was trying to paint the page somebody actually came for.
- The two faces the first screen is set in are asked for in the document rather than discovered inside the stylesheet, which takes a round trip out of the path to readable text.

- The poster designer became an editor. It drew one fixed layout with a handful of switches over it, so a board could change the colour of a poster and which of four pieces it carried, and nothing else: not where the title sat, not how large it was, not whether the room went above the hours, not whether there was a second picture. A design now starts from one of four templates, with the event already laid out in it, and from there everything on the sheet is a layer. Pick it up and drag it, size it by its handles or place it exactly with the arrow keys, set it in any of thirty four faces at any size, recolour it, turn it, send it behind something else, copy it or take it off. Add as many blocks of words, pictures and shapes as the poster needs, and put the link square wherever it belongs. Undo and redo cover everything, a drag counting as one step rather than a hundred, and the design is kept in the browser that made it so a board can come back to it. The same drawing puts the poster on the screen and into the file that downloads, so the preview cannot show one thing and the download another.
- The Continue button on the Discord linking page can be read again. It is written as a link, and a reading page sets the links in its prose in the primary colour, which outranked the button's own colour and painted the label in the primary colour on the primary gradient.
- The Sign out button in the sky band can be seen again. A cut secondary button is two layers, its own background being the border colour and a chamfered layer inset by the border width covering all but the ring, and the band told that inner layer to be transparent so that the sky would show through, which let the border colour fill the whole button, label and all. Each sky now names the colour it begins at, the way the night sky already did, and the band hands that colour to the button standing on it.
- A poster design that the browser cannot keep is now said out loud rather than lost. A picture is held inside the design, written as text, and the whole design has to sit in the few megabytes a browser gives a site, so a photograph straight off a phone was enough to push it over. The design then stopped being saved with nothing said, and a board member who reloaded the page found their work back at the last version that fit. A picture over two megabytes is turned down at the door with the reason, and a design the browser refuses is now a message asking the board to download the poster before they close the page.
- An address under /updates that no update was ever written for is no longer offered to search engines as a page of its own. The client knows which updates exist, because it builds them out of the markdown, and the server did not, so it was answering that any address of that shape was a real page worth keeping, with a canonical address pointing at itself. The server now reads the same filenames the client does.
- The page listing every organization no longer says there are none when VIA could not ask. A failed query became an empty list, and that was served as an indexable page headed "Every ECE student organization" with nothing under it. It answers 503 and asks a crawler to come back, as the event and organization pages already did.
- The capacity of a room is gone from every screen. The number in the locations table is not one anybody measured: it reads the same for very nearly every room on campus, so a board comparing two rooms by it learned nothing.

## 0.7.0 (2026-09-18)

- 

## 0.6.1 (2026-09-13)

- 

## 0.6.0 (2026-09-13)

- 

- Events stopped going missing from the calendar. A range of dates is a range of days, and a day is a whole day, but every listing that takes a start date and an end date compared them against the stored time as written, so an upper bound of the thirtieth of September was midnight that morning and threw away everything that happened on the day it named. That is why events went missing from the last week of some months on the calendar while sitting on the events page as usual, why nothing ever appeared in the Saturday column of the week view, and why the scheduler weighed a search window with its final day empty. Three more things went with it: the month grid stopped at the last day of the month rather than running in whole weeks, a day holding more than three entries hid the rest behind a count that could not be opened, which is what a term imported from a calendar file looks like, and the calendar opened on the week when the month is the view a board reads it in.
- Importing a calendar file into an organization no longer looks as though nothing happened. The panel was drawn only while the manual entry form was open, so there was nowhere to import from without first opening a form for an event nobody was entering, and it told the listing behind it nothing when it succeeded, so the events table went on showing what was there before. It has its own control now, and the table reloads.
- A repeat can be more than every week or every other week. Every so many weeks, since the interval was always stored and only two of its values were ever offered. Once a month, in the two forms that phrase actually means, a date in the month such as the fifteenth or a weekday of it such as the second Tuesday or the last Friday, with a month that has no such date left out rather than slid into the next one. And a set of dates picked one by one on a calendar the organizer clicks, each taking the hour and the length of the event, with nothing left out, because a date somebody chose is a date they meant.
- Several midterms can be taken off the exam schedule at once. A calendar imported under the wrong course codes, or a term that has ended, leaves a page of entries to clear, and clearing them one confirmation at a time is what boards were doing. Filtering or sorting the listing leaves the choice where it was.
- The tags an event can carry are kept by an admin rather than by a release. The eight the platform shipped with were written into the event form and into the events feed's filter panel, as two copies of the same array, so adding one meant a deploy. A tab on the admin page adds to the list and takes from it, and the listing says how many events each tag is on, because removing a tag takes it off every event that carries it.
- The About page carries a bug report form. Until now the only way to say that something on VIA was wrong was to write to the address at the bottom of that page, and most people do not write an email about a button. The form asks which part of the platform it was and one line about what happened, fills in the page the reporter was on, and takes everything else as optional. Nobody has to sign in to send one, no network address is recorded with it, and the reports stay in VIA's own database, where an admin reads them, closes them and can take the whole listing away as a spreadsheet file.
- The platform updates move into About, which is a set of tabs now, each with an address of its own. Every update keeps the page it has always had, and the old listing address leads to where the listing went.
- The delete controls on a midterm entry can be seen on the dark theme. The red they are drawn in was a dark maroon at about two to one against the page, which is legible behind white text on a solid button and invisible as a label or an outline, and both of those controls are drawn the second way. Every pair of colours a person reads is now held to the contrast ordinary text is meant to have.
- Smaller things across the site. The admin page's window picker was a white box on a dark page, because a native picker with no background of its own is painted by the browser rather than by the theme, and every one of them in the site is now checked for the same mistake. The tab holding it says what it is rather than saying Availability. The organization switcher on the dashboard has a row of its own, so an organization with a long description no longer pushes it across the header and out from under the pointer. The events feed calls what has already happened past rather than archived. The navigation names the dashboard My RSOs and rules off the board's entries from the ones every reader has. And a board member reading the feed has the way through to scheduling an event beside it.
- Two independent reviews of the Discord work were acted on in full. The most serious finding was in the account link: the value VIA handed to Discord and read back on the way in did not name the person who started the flow, so a crafted callback address linked an attacker's Discord account to somebody else's NetID. That value now names them, a callback opened with anybody else's cookie is refused, and a token VIA signed for that flow can no longer be presented as a sign in cookie. An organization's statistics, which carry what people wrote about its events, are now read by its board rather than by anybody signed in. The personal calendar address, the Discord authorization code and the link session identifier are taken out of the access log before a line is written, the two published ports are bound to the loopback address so the internal service API is not on the host's public address, the bot's database account is granted its own database rather than every database whose name happens to match it, and the personal calendar has a ceiling of its own so that guessing at addresses is slow. Several things the bot and the dashboard did were also wrong. Restoring a backup by its path alone restored nothing, which is exactly what the rollback does; a rollback that could not start the bot left the website down; saving an untouched event from the dashboard cleared its location note, and creating or editing a repeat dropped the note entirely; a cancelled event went on holding its room against the scheduler; editing one week of a repeat from Discord severed that week from the repeat even when nothing about its time had changed; deleting the last week of a repeat took the repeat away without telling the bot; and a personal calendar that followed no organization was answered with every event on campus. Cancelling a repeating event now asks which weeks it means, as editing and deleting already did. The dashboard shows the ratings and comments the API already answered with, and never who gave which rating. A cancelled event says so in the structured data search engines read. The calendar files VIA writes describe the campus time zone rather than only naming it, and the one the bot hands out arrives under a name a browser can save. The account page sends a signed out visitor to sign in, shows its times on the campus clock, and offers the optional linked roles step to somebody who did not take it when they linked. The privacy policy has a section on the Discord link, saying what is stored, how long it is kept, and how to remove it.
- The Discord bot is deployed with the website rather than on its own. It runs as a third container beside the database and the application, built from a checkout of its own repository next to this one at the tag written in `deploy/bot-release`, so which bot a given release of VIA runs is recorded in that release rather than decided on the server. One cutover deploys both: it refuses either checkout if it has uncommitted work, builds both images and backs up and proves the restore of both databases while the site is still up, then stops both containers, migrates the website and then the bot, starts the website and waits for it to report healthy, and only then starts the bot and waits for the same from it. Anything that fails after the window opens restores both databases, puts both checkouts back on their previous tags and starts both previous images. The bot has its own database and an account that reaches nothing else, created when the database container first starts, and it is given none of the website's own credentials, because everything it knows about events, memberships and people it asks the website for.
- The Discord bot can now do things on VIA for the people who use it, and every one of them is decided by the web platform rather than by the bot. A board member can postpone a meeting with a reason, cancel one and put it back, change what an event says, whether it is internal and the note at the door, ask the scheduler where a repeat could go, and create that repeat, all from Discord, and each of those runs the same code the dashboard runs, so the checks, the refusals and the record of the change are the same on both surfaces. Anybody can say they mean to go to an event, counted once however many times they say it, and somebody who has not linked their account is counted under a salted hash of their Discord identifier rather than under the identifier itself. Anybody who was there can rate an event from one to five and leave a comment, once per event, and the board reads the average, the count and the comments and never who gave which rating. A student can also subscribe to their own VIA calendar from their phone: the bot hands them one address carrying the events of the organizations they follow, with nothing internal and nothing that was called off in it, asking for a new address takes the old one away from anybody holding a copy, the address is stored only as a hash because it is the whole of the credential, and fetching it neither goes into a shared cache nor counts against the limits anonymous readers of the public API meet.
- A student can link their Discord account to their VIA account. The bot opens a link request for the Discord account that asked, and the person opens the address it sends them, signs in with their NetID if they have not already, and reads what linking does before they agree to it: the bot can act as them on VIA for the things they can already do on the website, it never reads their messages, and they can undo it at any time. An optional step publishes three facts to Discord, that the person is verified, whether they sit on a board, and the day they linked, so a server can hand out a role for them, and those facts are refreshed whenever their memberships change. The account page on the website shows whether a Discord account is linked and unlinks it with one confirmation, and unlinking from either side takes the facts back on Discord as well. A link request is good for ten minutes, it can only be finished by the Discord account it was opened for, and the authorization VIA keeps is encrypted at rest.
- Every change the Discord bot has to hear about now leaves an entry in the outbox, and the bot reads them in order from a cursor it keeps. Creating an event, editing or cancelling or deleting one, creating a repeat and editing or deleting part or all of it, importing a calendar of events or of exams, confirming or cancelling an exam, and adding or removing somebody from an RSO all write one entry carrying the kind of change, what it was about, and the object as it stands afterwards, so the bot can post without asking again. Where the change already runs inside a transaction the entry is written in that transaction, so an entry can never describe a change that was rolled back. Entries are kept for thirty days and removed after that, because a bot away for longer rebuilds what it knows from the reading endpoints instead.
- The internal service API now answers everything the Discord bot reads: the list of RSOs and one RSO with its next events, an RSO's membership for a board member reconciling roles in a bound server, the events feed with the same filters, listing ceilings and archive rule the website itself applies, one event with its series and its interest count, that event as a calendar file a phone can open, the midterm schedule, course and room search for autocomplete, the rooms in a building that no class, reservation or event has taken for a window, and the name and address behind a building code. An internal event is shown only to somebody the web platform agrees is a member of that RSO, and a request that acts as nobody never sees one. Every answer shape is written down in a committed file that the bot's repository copies, so a change to a shape is a change a reviewer can see.
- The tables the Discord bot will need are in place: a link between a Discord account and a NetID, the short lived session that makes one, an outbox of changes for the bot to read in order, interest in an event, feedback after an event, and a personal calendar subscription. Events can also now be cancelled rather than deleted, and can carry a location note. Nothing reads or writes any of it yet.
- The door the Discord bot will come through is in place. Requests under the internal service API prefix must carry the bot's service token, are answered 404 if they arrived through the reverse proxy or if no token is configured, and are refused last under load, alongside a board member's write. A request the bot makes for a person carries that person's Discord identifier, and the web platform resolves it to a NetID through the link table and applies exactly the rules the dashboard applies. There are no endpoints behind the door yet.
- An event can be cancelled rather than deleted. A cancelled event keeps its page, which says at the top that it was cancelled, and it moves from the upcoming feed into the archive, marked, from the moment it is cancelled rather than from its date. It is left off the lobby display, and it gives its room back, so the scheduler and the booking check treat the slot as free. The dashboard offers to cancel an event from its row and to restore one that was cancelled by mistake. The reason for the state is the Discord bot: an announcement edited to say "cancelled" needs something to point at, and a deleted event cannot tell the people who planned to attend that it was called off rather than mistyped.
- An event can carry a location note, the small thing a board changes at the door, such as which entrance to use. It is a field of its own on the event form, shown beside the room on the event page, and kept apart from the room so it is never mistaken for one.
- The event page says how many people are interested, once anybody is, and the dashboard's insights tab lists that count for every upcoming event. Interest is recorded by Discord's own control on a mirrored scheduled event and by the bot's button on an event card, and it is the count the removed RSVPs used to give.

## 0.5.2 (2026-09-03)

- VIA now degrades into honest refusals rather than into an out of memory kill. Under enough load, callers used to queue for a database connection without any bound, each holding a socket and a half built response while memory climbed until the kernel picked a victim, and because the server is shared with other applications the victim was not necessarily VIA. The connection queue is now bounded, three self calibrating signals decide when the platform is overloaded, and traffic is refused in order of increasing value: anonymous reads of the public feed first, and a board member's half written event last. A refused request is answered with a sentence saying VIA is busy and when to try again, as a small page for somebody who followed a link and as JSON for anything else. The health endpoint is never refused, so a deploy is still judged on whether the platform is actually up.
- Both containers now have a ceiling on the memory and the processor time they may take, the database sizes its buffer pool against that ceiling rather than against the whole machine, and connections can no longer be held open indefinitely saying very little. The keep alive window sits deliberately above the one the edge uses, so the origin is never the side that closes a connection the edge still believes is open, which is a known source of errors that nothing in the logs explains.
- Every request VIA turns away is now counted, and the admin page has an Availability tab that shows those counts by day and by reason with a sentence explaining each one. Counts are aggregated in memory and written once a minute, so recording a refusal never adds load at the moment load is the problem, and no address is stored in any form. The question the tab answers is whether readers are being turned away and how often, which is what decides whether any of the new limits need loosening.
- Bulk collection of the public listings is now throttled without closing them to anonymous readers. Two budgets apply to a caller who is not signed in: how many requests it may make, and, more importantly, how many rows it may be served in an hour. A request rate limit catches a client that hammers and misses one that paces itself overnight, while a row budget measures what was taken rather than how it was asked for. Signed in readers, lobby displays, the term calendar and search engine crawlers verified by their own domain records are all exempt, and the strongest consequence available is a one minute wait. Nothing is banned and nothing escalates, because a single campus address may be a lecture hall rather than a collector.
- The events feed's filter panel is answered by the server. Choosing which RSOs to show, or hiding internal events, used to be done in the browser, which meant the feed fetched every event in the term before it could draw the eighteen on the page, and the page count under the list was worked out from whatever had arrived. Both are now part of the query, so one page of results is one question asked of the database, the count is counted against the same filter, and a student narrowing the feed to one club is no longer served a term of events they never see.
- Asking a listing for more than it holds no longer works. Every list endpoint now has a ceiling on how many rows one request may take and how far into the results it may page, and those ceilings reach the database rather than stopping at the handler. Asking for more than the ceiling is quietly given the ceiling, since that answer is still correct, and asking for a page far past the end is answered with a sentence suggesting a narrower range of dates. Two bugs go with this: a limit that was not a number reached the database as nonsense and came back as a server error, and a limit of any size at all was accepted as written.
- The address VIA uses to tell one visitor from another is now the visitor's rather than the content delivery network's. It was reading the edge server's address, which put everybody arriving through a given edge into a single bucket, so one person failing to log in repeatedly could lock out everybody else behind that edge.
- A server error no longer publishes the underlying message to whoever provoked it. The messages that arrive there uninvited come from the database driver and name the host and the account it connects as.

## 0.5.1 (2026-09-03)

- Creating a repeating event works again. The create event form posts its times as the browser's date and time field holds them, with a T between the date and the hour and no seconds, and the server read them as though they were the shape the database writes, with a space. The hour came out as midnight and the length of the event came out as no number at all, so the insert failed and the organizer was shown the failed query across the top of the form. Both shapes are now read as the same time, and a time the server genuinely cannot read is answered with a sentence saying so rather than a database error. The same reading is applied when a repeat is edited, where it would otherwise have moved every week of the series to midnight.

## 0.5.0 (2026-09-02)

- The first download is a third of what it was. Every page of the site was in it, so a student opening the feed fetched the logistics dashboard, the intelligent scheduler, the poster designer and the markdown and QR code libraries before seeing a single event. Each page is now its own file, fetched when somebody opens it, which takes the first download from 404 kB to 106 kB, or 122 kB to 38 kB over the wire. A page that cannot be fetched, which happens to a tab left open across a deploy, now says so and offers to reload instead of leaving a blank space.
- A visit no longer waits on the answer to who is looking before drawing anything. The whole screen, and the feed's own request, used to sit behind that round trip. Pages that exist only for somebody signed in still wait, because drawing one before the answer arrives sends a board member to a login page they are already past. The account area of the navigation bar stays empty until the answer comes, rather than showing Sign in and correcting itself a moment later.
- Responses are compressed, and the ones that can be cached say so. The CDN now fetches from Champaign compressed, so every cache miss travels a fraction of the bytes. Nothing the API answers is stored by a shared cache unless the route says otherwise, because most of it depends on who is asking, and the two that do not, the kiosk listing and the term calendar, can now be answered at the edge, as can the sitemap and the files crawlers read. The document itself is checked every time, because it names the files this build produced.
- The events feed has an index to read from. Every feed query reads public events by when they start and sorts them the same way, and none of that had an index, so each one read the whole table and sorted it. It did not show while events were entered one evening at a time; recurring events turn a term of weekly meetings into about sixteen rows apiece, so the table now grows by the term.
- Two dependencies carrying high severity advisories are updated, one of them the MySQL driver, where the advisory is an authentication plugin downgrade that leaks the database password in plain text. Both were fixed within the versions already allowed, so nothing about how they are used changes.

- Events can repeat. A weekly meeting, a fortnightly design review or office hours on a Tuesday and a Thursday is now one thing to set up rather than fifteen, and it can be created three ways. The event form has a repeat: every week or every other week, on the days chosen, until a date that starts at the end of instruction and can be changed. A calendar file whose entries carry a repeat rule is expanded on import, where before a repeating entry was imported as its first occurrence and nothing else, which is how a board could import a term and end up with one event. And the scheduler can be asked which evening works for the rest of the term rather than which evening works next week: it scores a weekday and an hour across every week it would run, says how many of those weeks are clear, and creates the whole series when a recommendation is accepted. The repeat runs to the end of instruction and skips the weeks with no classes, read from a university calendar VIA now keeps, and every screen shows the date it arrived at so an organizer can correct it. Editing or deleting one week of a series asks whether that week, that week onwards, or the whole series was meant, and a week changed on its own stays changed when the rest of the series moves later.
- Editing an event that had a room no longer fails. The check for a double booking called a helper the module never imported, so saving any event with a room and a time answered with a server error instead of saving. Editing an event from the dashboard also no longer clears its room: the listing behind that form did not carry the room's identifier, so saving an untouched form removed the booking.

- RSVPs are removed. The RSVP card on the event page, the Going and Maybe buttons on every event card, and the two endpoints behind them are gone, along with the calls the browser made to them and the copy elsewhere on the site that offered the feature. Anything still asking for those two addresses is answered with a 404, rather than being led to believe it recorded something. The RSVPs table is left where it is, holding what was recorded, since dropping it would throw away the only copy and nothing reads it now.
- The events feed opens on what is still to come. It listed every event VIA had ever held, oldest first, so the first page a student landed on was full of talks and socials from previous terms and the events happening this week were pages deep. The feed now starts at the beginning of the campus day, which keeps an event that began this morning on the page for the rest of the day, and everything before that moves into an archive. The archive is a click away in the filter panel, under When, and it reads most recent first, which is the order somebody looking back through the term wants. The calendar is unaffected and still shows whichever week or month it is pointed at, including the ones that have passed. The front page that search engines and assistants read now lists upcoming events too, which is what its heading always claimed.

## 0.4.0 (2026-09-01)

- Every time on the site is now campus time, and one clock decides it. A stored time is the wall clock the organizer typed, and it was being read back by the database driver as an instant in whatever zone the server container had started in, published as UTC, then rendered by the browser in whatever zone the reader was in. Neither of those two conversions is campus time, so the hour an event showed depended on where it was read from and on how the container happened to be started. Times now leave the API with the campus offset attached, which names one instant, and the pages render that back on the campus clock, so a student reading the feed from home over the winter break is shown the hour they would have to turn up at in Champaign. Both containers also run on campus time, so a timestamp the database writes for itself sits on the same clock as the event times beside it.
- Whether a midterm reads as upcoming, ongoing or past is now decided against the campus clock. It was decided by MySQL's NOW(), which is the database container's clock, so the answer was five or six hours away from the exam and the listing hid or kept the wrong entries around the turn of the day. The same fix applies to the upcoming events on the kiosk and to the confirmed midterms on the calendar.
- The scheduler recommended the wrong times. It published each slot converted to UTC, so a slot found for six in the evening was offered as an hour that depended on the container rather than on the campus clock, and it asked for the midterms in its lookahead window using a bound converted the same way, so it weighed the wrong window of exams. Both now work in campus wall clock.
- The calendar asked the server for the wrong week. The week it displayed was converted to UTC before being sent, which moves it to the previous day for any reader west of UTC, so the week shown and the week fetched could disagree. Events also sat in the wrong column and at the wrong height on the week grid, because the grid placed them by the reader's clock rather than the campus one, which slid a late evening event into the next day.
- An event edit form is filled with the hour the organizer typed. It was filled from the published instant read in the reader's own zone, so opening an event and saving it without touching anything moved it.
- A midterm can be deleted, not only cancelled. Cancelling keeps the row, which is right for an exam that was scheduled and then called off, and wrong for one that should never have been listed, which until now stayed in the admin listing forever. Deleting asks for confirmation first. One thing is worth knowing and the page says so before you delete: a midterm that came from the HKN calendar is added back by the next import of that calendar, because the import matches entries on the identifier the calendar gave them and finds nothing. Cancelling is the way to keep one of those off the schedule for good.
- Importing midterms from a calendar file is open to global admins and to anyone who sits on an RSO board, rather than to global admins alone, and the panel is now on the midterm schedule as well as on the admin page. It was previously reachable only from the admin page, which a board member cannot open, so from a board member's seat the feature did not exist at all, even though the boards are the people who read that schedule. An ordinary member still cannot import, and neither can an editor. Importing events is unchanged and still needs board or editor access to the RSO it writes to. Every import still previews first, so nothing is written unseen.
- The calendar import panel now says when it has written something, and the listing behind it reloads. It previously left the table showing the schedule as it stood before the import, which read as an import that had done nothing.
- Deleting a midterm is open to global admins and to anyone who sits on an RSO board, rather than to global admins alone. The schedule belongs to no single RSO, so there is no RSO to be on the board of for a given exam, and sitting on any board is the bar. Boards are the people who plan around this listing and so the people who notice what is wrong with it, and a board that spotted a duplicate previously had nobody to tell but an admin. An ordinary member still cannot delete, and neither can an editor, whose remit is that RSO's own events. Board members cannot open the admin page, so for them the control is on the midterm schedule itself, where it reaches the exams that listing shows. A global admin still reaches the whole history from the admin page.

## 0.3.0 (2026-09-01)

- Events and midterms can be imported from an .ics calendar file. The file is read in the browser and its text is posted, so there is no upload endpoint and nothing is written to disk. An import previews first: it lists what it would create or update, which room each entry resolved to, and what it could not read, and writes nothing until that is confirmed. Importing the same file again updates what the first import created rather than duplicating it, matched on the identifier the calendar gave each entry. Rows entered by hand carry no such identifier and are never touched. A location is only claimed as a known room when the text names both a building and a room number, so ECEB 1002 resolves and a bare 1002 stays as written. Midterm entries have their course read out of the title and are imported as confirmed, since HKN is the authority; titles naming no known course are reported rather than guessed at.
- Midterm crowdsourcing is gone. The votes table and the trigger that confirmed a midterm once its score reached five are dropped, along with the vote endpoint and the vote column in the listing. Midterms now come from HKN, so a midterm can have no submitting user.
- An RSO board can add people who have never signed in. Paste a list of NetIDs or Illinois addresses and the roster is recorded straight away; anyone without an account gets a placeholder row carrying no name and no email, marked as invited, and they find themselves a member the first time they sign in. Entries that cannot be a NetID are reported back rather than silently dropped.
- An event's location is now optional, and can be a room VIA knows about or free text for anywhere else. The venue finder that asked for expected attendance and ranked rooms by capacity is gone, replaced by a search box that understands building codes: typing ECEB, eceb1002 or electrical all find rooms stored under the full building name. Events with no room now appear in the feed and on the kiosk, which they would not have done before, because every listing query joined the Locations table and an inner join drops exactly those rows.
- Standard security response headers are sent on every response: nosniff, SAMEORIGIN framing, a referrer policy, and strict transport security in production only. The framework banner is no longer advertised. No content security policy is set yet, because writing one by guesswork either blocks the site or permits everything.
- Member email addresses are shown only to the board of that RSO and to global admins. They were previously returned to any signed in user, which at UIUC is every student.
- The three endpoints that can be asked to do a lot of work per request are now bounded: a calendar import is capped at a thousand entries and rate limited, the calendar endpoints accept a larger request body than the rest of the API so that a real year of events does not fail with an opaque error, and a pasted roster is capped at two hundred people.
- A pasted roster is no longer split on spaces. A spreadsheet column of names beside addresses used to become one account per word, so pasting Jane Doe jdoe2@illinois.edu created accounts called jane and doe. A field containing spaces is now reported back instead of guessed at, and a header row is ignored.
- The circuit background is reworked. It no longer animates on its own: the previous version spawned a pulse somewhere on screen about twenty five times a second, which competed with the page for attention. The board is now still and faint until the cursor reaches it, and touching it sends current out from the nearest pad, spreading through the traces and fading as it goes, so the only motion is where you are already looking. Traces are routed with 45 degree chamfered corners the way a real board is, they take their resting colour from the active theme, and they carry current in Illinois orange. The canvas fades out behind the content column. Reduced motion is respected, and the animation loop stops when nothing is lit and when the tab is hidden, which matters for the kiosk that runs this on a lobby screen indefinitely.
- The site can be indexed. Every address used to return the same empty document with the same title, nothing in the body, and no link to any event page, so search engines had nothing to rank and crawlers that do not run JavaScript, which is most of the ones behind assistants, saw an empty site. The server now fills the document in before sending it: a title and description per page, a canonical address, sharing tags with a real card image, schema.org structured data, and a readable summary of the page that the application replaces when it starts.
- Event pages carry schema.org Event data, including start and end times with the campus timezone offset, the organizing RSO, and either the room or an online location. This is what search engines need to list an event with its date and place, and what assistants read instead of rendering the page.
- The sitemap and robots.txt are generated rather than shipped as fixed files. The sitemap now lists every public event rather than four pages, and uses absolute addresses, which the sitemap protocol requires and the previous file did not, meaning every entry in it was rejected. Internal events are excluded and carry noindex, as do the signed in areas.
- There is an llms.txt describing the site for assistants, and robots.txt names their crawlers explicitly, since several only index a site that says so.
- Built assets are served with a long cache lifetime and everything else revalidates hourly.
- The privacy policy and the terms are rewritten. The old privacy policy stated that the platform ran on Google Cloud with its database on Cloud SQL, which stopped being true when VIA moved to a single virtual server, and it described neither the placeholder records created when a student organization adds a member before that person has signed in, nor the database backups, nor the fact that event pages are now indexed by search engines and read by search assistants. All of that is now stated. The terms gain a plain statement that VIA is not a University service and is not endorsed by it, a licence covering the content organizations post, a copyright complaints route, disclaimers and a limit of liability, an indemnity, and Illinois governing law with a thirty day period to settle a dispute by asking first.
- The beta banner is gone from the top of every page.
- The "VIA is Live" platform update is removed.
- The About page carries a contact address, and the privacy policy and terms name it directly for deletion requests, copyright complaints, security reports and disputes, rather than pointing at a page that had no way to reach anyone on it.
- The background pollers can be switched off with POLLERS_ENABLED, so a local instance does not scrape Course Explorer and Ad Astra every time it starts. They stay on unless the value is exactly false or 0.
- A preview database for local development, with its own compose file, its own port and its own fixtures. It is built by npm run dev:db and described in docs/development.md.

## 0.2.3 (2026-08-31)

- The database no longer sits on the shared network named internal. That network carries every stack on the production host, Compose gives each service the alias of its own name on it, and three stacks there run a service called db, so the name resolved to three different servers and each connection was a draw between them. The database now sits on a network private to this stack under the name via-db, which no other stack answers to. This is the cause of the intermittent access denied errors on the public feed.
- A failed database restore now reports the error the client gave. The dump was piped into the client with no error handler on that stream, so when the client rejected a statement and stopped reading, the resulting write failure became an uncaught exception that killed the process before the exit code and the message could be read. Every restore failure surfaced as an unexplained EPIPE, including during a cutover and during a rollback.

## 0.2.2 (2026-08-30)

- The compose file now declares the external network named internal that the reverse proxy uses to reach the application by service name. It was previously an edit that existed only on the production host.
- A test guards the compose file against the three ways it has drifted or could drift: a schema mount into docker-entrypoint-initdb.d, a missing host backup mount, and a missing external network. The gate runs it.

## 0.2.1 (2026-08-30)

- The release gate now runs on pull requests to main and on version tags, and its three jobs are required by branch protection.
- The database schema is defined by migrations under server/db/migrations, applied by a locking migration runner, with a drift check in the gate.
- Deploys go through scripts/cutover.sh, which takes a verified backup, applies migrations, checks health, and rolls back on failure.
- The health endpoint is a real readiness check, and it reports the application version and the applied migration version.
- The root, server and client packages report one platform version, kept equal by a test in the gate.
- User facing copy and documentation no longer contain em dashes or en dashes, enforced by npm run check:language.
